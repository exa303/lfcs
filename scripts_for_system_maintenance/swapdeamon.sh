# Idea: write a program that constantly monitors swapsapce usage. in case
# of shortage, creates swap file and adds it to the system, widouth removing previuos ones.
# and frees space when no swap is needed anymore. if there is some funky stuff happening program
# notifies admin via MTA.